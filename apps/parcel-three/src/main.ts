import { startBundlerThreeSmoke } from '../../../apps/shared/bundlerThreeSmoke';

startBundlerThreeSmoke({
  appId: 'parcel-three',
  host: 'parcel',
  backend: 'copc-js',
  bundlerLabel: 'Parcel',
});
