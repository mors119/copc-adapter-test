import { startBundlerCesiumSmoke } from '../../../apps/shared/bundlerCesiumSmoke';

startBundlerCesiumSmoke({
  appId: 'rollup-cesium',
  host: 'rollup',
  backend: 'copc-js',
  bundlerLabel: 'Rollup',
});
